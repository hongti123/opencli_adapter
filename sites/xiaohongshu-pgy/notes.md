## 2026-08-14 by Codex

- Strategy: `UI_SELECTOR`; contract: `visible-ui`.
- 未登录访问 `/solar/post-trade/content-manage` 会回到蒲公英首页。
- 登录弹窗背景和弹窗内同时存在“账号登录”，选择器必须限定在 `#login-form` 所属弹窗容器。
- React 页面上先标记节点、再调用 `page.click()` 可能不触发处理器；登录、账号 Tab 和导出均在同一次 `page.evaluate()` 中派发鼠标事件并 `click()`。
- 页面历史上导出过 `笔记-投放数据.csv`，2026-08-14 实测导出为 XLSX；适配器同时接受 `.csv` 和 `.xlsx`，并保留真实扩展名。
- 2026-08-14 实测端到端成功：下载 `2026-08-14新媒体蒲公英.xlsx`，大小 1,245,137 字节；测试时关闭共享目录复制。
- 原技能按“下载目录最新 XLSX”取文件，可能误拿其他业务文件；适配器只接受命令开始后产生且大小稳定的新 CSV/XLSX。
- 导出触发存在间歇性，适配器会在总超时内最多重试 3 次，每次等待 45 秒；不会无限重试。
